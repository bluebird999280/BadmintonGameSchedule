import ScheduleContainer from "feature/schedule/container"

function SchedulePage(): JSX.Element {
  return (
    <div style={{ display: "flex", justifyContent: "center" }}>
      <ScheduleContainer />
    </div>
  )
}

export default SchedulePage
